import Dog from './Dog';

// mixed array
const mixedArra: (number | string)[] = [];
mixedArra.push('one');
mixedArra.push(2);

// private, readonly, public
const dog1 = new Dog('Good doggy', 1, 'Lisa');
